# Agent Zero SwarmOS - Docker Swarm Configuration
# Copy this file to .env.swarm and customize for your deployment

# =============================================================================
# NETWORK CONFIGURATION
# =============================================================================

# DHT Topic for peer discovery (all nodes must use the same topic)
# Default: agent-zero-swarm
SWARM_TOPIC=agent-zero-swarm

# Number of agent nodes to spawn (default: 2)
AGENT_COUNT=2

# =============================================================================
# BRIDGE NODE PORTS (External -> Container)
# =============================================================================

# Bridge A HTTP API port
BRIDGE_A_PORT=3000

# Bridge B HTTP API port
BRIDGE_B_PORT=3001

# =============================================================================
# AGENT NODE PORTS
# =============================================================================

# Agent A Web UI port
AGENT_A_PORT=8080

# Agent B Web UI port
AGENT_B_PORT=8081

# =============================================================================
# LLM CONFIGURATION (Applied to all agents)
# =============================================================================

# Chat model provider (openai, anthropic, ollama, google, etc.)
# CHAT_MODEL_PROVIDER=openai

# Chat model name
# CHAT_MODEL_NAME=gpt-4o

# API keys (set these securely, don't commit!)
# OPENAI_API_KEY=sk-...
# ANTHROPIC_API_KEY=sk-ant-...
# GOOGLE_API_KEY=...

# =============================================================================
# AGENT IDENTITY (Per-node customization in compose override)
# =============================================================================

# Default agent name prefix (nodes will be named: Agent-A, Agent-B, etc.)
AGENT_NAME_PREFIX=Agent

# =============================================================================
# SECURITY
# =============================================================================

# Flask secret key (generate with: python -c "import secrets; print(secrets.token_hex(32))")
# FLASK_SECRET_KEY=

# Optional: Basic auth for web UI
# AUTH_LOGIN=admin
# AUTH_PASSWORD=changeme

# MCP server authentication token
# MCP_SERVER_TOKEN=

# =============================================================================
# RESOURCE LIMITS (Per container)
# =============================================================================

# Memory limit per agent container
AGENT_MEM_LIMIT=2g

# CPU limit per agent container (1.0 = 1 core)
AGENT_CPU_LIMIT=1.0

# Memory limit per bridge container
BRIDGE_MEM_LIMIT=256m

# CPU limit per bridge container
BRIDGE_CPU_LIMIT=0.5

# =============================================================================
# PERSISTENCE
# =============================================================================

# Data persistence mode: 'volume' (Docker volumes) or 'bind' (host paths)
PERSISTENCE_MODE=volume

# If PERSISTENCE_MODE=bind, base path for data directories
# HOST_DATA_PATH=/var/lib/agent-zero-swarm

# =============================================================================
# LOGGING
# =============================================================================

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Max log file size before rotation
LOG_MAX_SIZE=10m

# Number of log files to keep
LOG_MAX_FILES=3
